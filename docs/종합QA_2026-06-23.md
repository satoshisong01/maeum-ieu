# 마음이음 출시 전 종합 QA 리포트 (2026-06-23)

> 총책임자 관점 A–Z 점검. 검증_플레이북.md 기준 + 신규 코드 보안 리뷰(에이전트).
> 발견 → 수정 → 재검증 → 기록 사이클로 진행.

## 0. 요약

| 영역 | 결과 |
|------|------|
| 정적·단위 (tsc / vitest / safety-regression / build) | ✅ PASS |
| DB 민감정보 암호화 (저장 암호문 ↔ API 복호) | ✅ PASS |
| 라이브 판단 (계정생성 → 대화 → 인지평가 적재) | ✅ PASS (1차 차단 발견 → 수정 후 통과) |
| 동의 게이트 (미동의 차단) | ✅ 작동 |
| **pro 전문가 검진** (채점 판단 + 라이브 파이프라인) | ✅ PASS |
| **라이브 응급→보호자 알림** (감지→notify→채널) | ✅ PASS |
| **30턴 풀사이클 + 비용·지연(Phase E)** | ✅ 클린 / 턴당 ~$0.006 / 지연 4.1s |
| 신규 코드 보안 리뷰 16건 | 🔧 7건 수정 / ⚠️ 4건 운영조치 / ✅ 5건 수용 |

**출시 차단 잔여 항목**: C-2(Vercel NEXTAUTH_SECRET 확인), C-1(노출 키 로테이션), C-3(DB TLS). 코드 레벨 차단 이슈는 모두 해소.

---

## 1. 정적·단위 (Phase A)

- `tsc --noEmit` — 에러 0 (수정 후 재검증 포함)
- `vitest run` — **196/196 통과**
- `safety-regression.ts` — **121/121 통과** (응급 감지 회귀 0)
- `next build` — 성공 (/consent 라우트 포함)
- prompt-leak 감사 — clean

## 2. DB 민감정보 암호화

profile PATCH → DB 원본 → GET 복호 라운드트립 실측:

- 저장: `guardianPhone`, `guardianEmail` 모두 `enc:v1:…` 암호문, **평문 미노출**
- 복호: GET 시 원래 평문으로 정확히 복원
- 방식: AES-256-GCM (`lib/crypto.ts`), 키 `ENCRYPTION_KEY`
- 결론: **저장은 암호문, API는 복호 — 정상**

## 3. 라이브 판단 (Phase B)

- e2e-roles(페르소나 LLM 운전) user 30턴 1차 → **동의 게이트가 /consent로 막아 0턴 실행**
  - "30턴 클린"은 카운터가 전부 0이라 거짓 통과였음(대화 자체가 없었음)
  - cognitive_assessments 0행도 같은 원인 (분석기 버그 아님)
- **수정**: e2e-roles 로그인에 동의 화면 통과 추가 (`scripts/e2e-roles.mjs`)
- 재검증(12턴): 동의 통과 → 실제 대화 → **인지 평가 3건 적재**
  (orientation_time / memory_immediate / memory_delayed, 정상 응답이라 score 0 = 올바른 판단)
- 부수 발견: 12턴 중 **FALLBACK(회피) 1건** — 회상 과제 중 AI가 다른 주제로 살짝 이탈.
  품질 미세 이슈(차단 아님), 다음 사이클에서 페이싱 점검 대상.

## 4. 보안 리뷰 (신규 세션 코드)

### 🔧 수정 완료 (7)

| ID | 내용 | 수정 |
|----|------|------|
| C-4 | 동의 게이트가 `/api/chat` 직접 호출로 우회 가능 | chat 라우트에 user 모드 동의 확인 추가(403) |
| H-1 | 비밀번호 변경 시 상한 미검증(bcrypt DoS) | profile PATCH `8~128자` 제한 |
| H-2 | 이메일 수신자 형식 미검증(복호 실패 시 enc:… 발송) | `sendEmergencyEmail` 이메일 정규식 검증 |
| H-3 | webhook이 http 허용(응급 발화 평문 전송) | emergency-notify + profile **https 전용** |
| H-5 | 가입 에러 로그가 전체 예외(DB URL 등) 노출 | `e.message`만 로깅 |
| L-1 | 이메일 HTML에 사용자명 미이스케이프 | `esc()` 적용 |
| M-1 | ENCRYPTION_KEY 미설정 시 평문 저장 무경고 | 운영 환경 1회 경고 로그 |

### ⚠️ 운영/사용자 조치 필요 (4)

| ID | 내용 | 조치 |
|----|------|------|
| C-1 | `.env`에 실제 운영 비밀 평문 존재(채팅서 노출된 Gmail 앱비번 등 포함) | **노출 키 로테이션** 권장: Gmail 앱비번 재발급, 가능하면 Gemini/Firebase/RDS/Upstash 키도 |
| C-2 | `NEXTAUTH_SECRET` 로컬 플레이스홀더 | 로컬은 실값으로 교체함. **Vercel 환경변수가 실랜덤값인지 반드시 확인**(플레이스홀더면 JWT 위조 가능) |
| C-3 | `DATABASE_SSL_NO_VERIFY=1`(DB TLS 검증 off, MITM) | RDS CA 번들 적용 후 플래그 제거(인프라 작업, 기존 트레이드오프) |
| H-4 | 전문가 검진 Q&A 조회가 시간창(시작~+25분)이라 일상대화 섞일 수 있음 | 검진 메시지에 examMode 플래그/정확한 ended_at 기록(기존 검진 로직, 신중 수정 필요) |

### ✅ 수용/저위험 (5)

- M-2 webhook 저장 시 DNS rebinding(발송 시점 lookup이 방어함)
- M-3 rate-limit Upstash 장애 시 인메모리 폴백(fail-open) — 응급앱 특성상 허용 가능
- M-4 로그인 타이밍으로 계정 존재 추론(next-auth 기본 동작)
- M-5 감사 로그 ID Date.now 충돌 가능(기능 영향 없음)
- L-2/L-3 webhook 발화 외부 전송 / WebView userId 전달 — 둘 다 설계상 의도(동의 범위·FCM 토픽)

## 5. 동의·프라이버시

- 동의 게이트 작동 확인: 미동의 신규/기존 user → `/consent` 강제
- **참고**: 기존 user 계정 전원 `consentedAt=null` → 다음 로그인 시 1회 동의 거침(의도된 동작)
- 일상 대화 비공개 원칙: 보호자/전문가는 결과 요약 + 응급 발화만(동의서 명시). H-4는 이 원칙의 경계 케이스로 후속 보강 권장.

## 6. 재검증 (수정 후)

- tsc 0 / vitest 196 / safety-regression 121 — 회귀 0
- 라이브 12턴 재실행 — 인지 평가 적재 확인

## 6.5 추가 심층 검증 (2차, 2026-06-23)

총책임자 관점에서 1차에 빠졌던 영역을 추가 검증.

**A. pro 전문가 검진**
- 채점 판단(실제 프로덕션 채점기 `scoreDomainAnswer`, Gemini): 시간지남력 정답5/오답0, 계산 정답5/오답0, 판단력 정답2/오답0 — **정답>오답 정합 3/3**
- 라이브 파이프라인: pro+환자 연결 → 검진 시작(200) → 인사(CIST 문항) → 답변 → 다음 영역 진행 → `exam_item_score` 적재 확인. 7영역 상태머신 정상 진행.

**B. 라이브 응급→보호자 알림 전체 플로우**
- 실제 채팅으로 자해 발화 전송 → **감지 emergencyLevel=3(suicidal)** → notifyGuardian → **webhook 발송 성공(notifiedAt 기록)**
- 실기기 FCM/Discord/이메일 전달은 이전 세션에서 확인됨 → 감지·알림 체인 전 구간 검증 완료

**C. 30턴 풀사이클 + 비용·지연 (Phase E)**
- 30턴: 빈응답·누출·모드혼입·기억실패·이상감지·네트워크 **전부 0 → 전 구간 클린**
- 지연: 평균 4.1s · p95 4.5s · 최대 6.3s (텍스트, thinkingBudget 256)
- 비용: companion 턴당 in~10.9k/out~71, analyzer-lite(2.5)+analyzer(3.5) 하이브리드 적용 확인.
  30턴 합계 ~595k tok ≈ **턴당 ~$0.006(₩8)** *(가격 근사치)*. 히스토리 50개 상한으로 입력 토큰 발산 없음.

## 7. 액션 아이템 (우선순위)

1. **[출시 전 필수]** Vercel `NEXTAUTH_SECRET` 실랜덤값 확인 (C-2)
2. **[권장]** 노출 키 로테이션 — 최소 Gmail 앱비번 (C-1)
3. **[인프라]** DB TLS 검증 복구 — RDS CA 번들 (C-3)
4. **[후속]** 검진 Q&A 일상대화 경계 보강 (H-4)
5. **[품질]** 회상 과제 중 회피 페이싱 점검 (e2e FALLBACK 1건)

---

### 변경 파일 (이번 QA 수정)
- `app/api/chat/route.ts` — 동의 게이트(C-4)
- `app/api/users/profile/route.ts` — webhook https + 비번 상한(H-3/H-1)
- `lib/chat/emergency-notify.ts` — webhook https(H-3)
- `lib/notify/email.ts` — 수신자 검증 + HTML 이스케이프(H-2/L-1)
- `app/api/auth/signup/route.ts` — 에러 로그 최소화(H-5)
- `lib/crypto.ts` — 키 미설정 경고(M-1)
- `scripts/e2e-roles.mjs` — 동의 화면 통과(테스트 도구)
- `.env` — NEXTAUTH_SECRET 로컬 실값 교체(C-2 로컬분)
